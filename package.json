{
  "name": "forma-resi-ifc",
  "version": "0.1.0",
  "private": true,
  "description": "Parametric residential building design → IFC generator (site, architecture, structure, mechanical, plumbing, electrical) built on ifc-lite's IfcCreator",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test 'src/**/*.test.ts'",
    "typecheck": "node scripts/typecheck.mjs",
    "gen": "node src/cli/main.ts",
    "build": "node scripts/build.mjs",
    "samples": "node src/cli/main.ts --all-presets --out dist/samples"
  },
  "license": "MPL-2.0"
}
